diff --git a/src/freedreno/vulkan/tu_cmd_buffer.cc b/src/freedreno/vulkan/tu_cmd_buffer.cc
index 3a7dc412c71..7890123abcd 100644
--- a/src/freedreno/vulkan/tu_cmd_buffer.cc
+++ b/src/freedreno/vulkan/tu_cmd_buffer.cc
@@ -3725,8 +3725,12 @@ tu_CmdBindVertexBuffers2(VkCommandBuffer commandBuffer,
                                         pStrides);
    }
 
+   /* Debug: Log VB size for Falcon */
+   if (getenv("TU_DEBUG_FALCON"))
+      mesa_logi("Falcon: max_vbs_bound=%d, size=%d", 
+                cmd->state.max_vbs_bound, 4 * cmd->state.max_vbs_bound);
+
    cmd->state.vertex_buffers.iova =
       tu_cs_draw_state(&cmd->sub_cs, &cs, 4 * cmd->state.max_vbs_bound).iova;
